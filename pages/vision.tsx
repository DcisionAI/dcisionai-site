export default function VisionPage() {
  return (
    <section className="min-h-screen flex flex-col items-center justify-center text-center px-6 py-20">
      <h1 className="text-5xl font-serif font-semibold text-brand-accent">
        We believe <span className="text-brand-accent/80">decisions shape destiny.</span>
      </h1>
      <p className="mt-6 max-w-xl text-lg text-brand-muted">
        So we’re rebuilding how they’re made — with agents, data, and protocol-based intelligence.
      </p>
    </section>
  );
}